<div class="list-books-container">
  <h1 class="list-books-title">Track Books</h1>
  {{#if errMessage}}
  <div class=""><p style="color: red;">{{errMessage}} </p></div>
  {{/if}}
  <div class="">
    <a href="/book/list">list book</a>
  </div>
  {{#if books.length}}
  <table class="table table-bordered">
    <thead>
      <tr>
        <th scope="col">image</th>
        <th scope="col">title</th>
        <th scope="col">author</th>
        <th scope="col">Action</th>
      </tr>
    </thead>
    <tbody>
      {{#each books}}
        <tr>
          <td class="td-image">
      <a href="/book/book-details/{{this.id}}" class="details-button">
            <img
            class="list-image"
              src="data:image/png;base64,{{this.image}}"
              alt="{{this.title}}"
            />
          </td>
      </a>
          <td class="td-title">
      <a href="/book/book-details/{{this.id}}" class="details-button">
            {{this.title}}</a> </td>
          <td class="td-author">{{this.author}}</td>
          <td class="td-button">
            <form action="/book/restore/{{this.id}}" method="POST">
              <button type="submit" class="list-restore-button">Restore</button>
            </form>
            <form action="/book/destroy/{{this.id}}" method="POST">
              <button type="submit" class="list-delete-button">Delete</button>
            </form>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
  {{else}}
  <p>{{message}}</p> 
{{/if}}
</div>
